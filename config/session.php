<?php defined('SYSPATH') or die('No direct script access.');

return array(
	'cache' => array(
		/**
		 * Cache settings for session storage.
		 *
		 * string   group           configuation group name
		 * string   prefix          Prefix of the cache key
		 * integer  expires         TTL for session (not COOKIE)
		 * boolean  serialize_json  Serialize using JSON
		 */
		//'group'          => 'memcache',
		//'prefix'         => 'session_',
		//'expires'        => 3600,
		//'serialize_json' => TRUE,
	),
);
